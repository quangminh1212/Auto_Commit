import os

# Default directory to watch (current directory)
DEFAULT_WATCH_DIR = os.getcwd()

# Default commit interval in minutes
DEFAULT_INTERVAL = 60

# Default commit message template
DEFAULT_COMMIT_MESSAGE = "Auto commit at {datetime}"
